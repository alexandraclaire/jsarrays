//create a vae with an empty array

var colorList = [];

//add one item to the colorList using push method

colorList.push('Purple');

//push 2 more colors to the end of the list. 

colorList.push('Pink', 'Blue');

//Adding colors to the beginning of the list.

colorList.unshift('Orange');

colorList.unshift('Red', 'Green');

//to see what that did add a printlist function:

printList(colorList);


